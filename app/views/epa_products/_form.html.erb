<%= form_with(model: epa_product) do |form| %>
  <% if epa_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(epa_product.errors.count, "error") %> prohibited this epa_product from being saved:</h2>

      <ul>
        <% epa_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :product_dimensions %>
    <%= form.text_field :product_dimensions %>
  </div>

  <div class="field">
    <%= form.label :package_dimensions %>
    <%= form.text_field :package_dimensions %>
  </div>

  <div class="field">
    <%= form.label :need_assembly %>
    <%= form.check_box :need_assembly %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
